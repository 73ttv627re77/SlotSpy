name: slotspy
description: Gym slot cancellation monitor — never miss a gym slot again.
publish_to: 'none'
version: 1.0.0+70

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  sqflite: ^2.4.1
  path: ^1.9.0
  flutter_local_notifications: ^18.0.1
  url_launcher: ^6.3.1
  audioplayers: ^6.1.0
  intl: ^0.19.0
  http: ^1.2.2
  uuid: ^4.5.1
  wakelock_plus: ^1.2.8
  home_widget: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
